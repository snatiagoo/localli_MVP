/*
import { getCurrentBusiness } from "@/lib/dal";

// Placeholder — the real weekly suggestion cards get built in a later
// build step. This just proves the auth + DAL gating works end-to-end.
export default async function DashboardPage() {
  const business = await getCurrentBusiness();

  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <p>Bienvenido, {business?.name ?? "negocio"}. Aquí irán tus sugerencias semanales.</p>
    </main>
  );
}
*/
