// Placeholder — the real multi-step wizard (basics/menu/audience/logistics)
// gets built in the next build step. This just proves the auth + DAL
// gating works end-to-end.
export default function OnboardingPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <p>Aquí irá el proceso de configuración de tu negocio.</p>
    </main>
  );
}
